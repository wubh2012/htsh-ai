services:
  contract-audit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: contract-audit
    ports:
      - "9999:9999"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9999/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
